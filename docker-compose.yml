
services:


  backend:
    build: ./backend
    container_name: agrilog-backend
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGODB_URI=mongodb+srv://Vercel-Admin-GreenhouseCibatu:SMSRiJX1G97b41ZD@greenhousecibatu.ijrkd2f.mongodb.net/?retryWrites=true&w=majority
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GEMINI_LIVE_MODEL=${GEMINI_LIVE_MODEL:-gemini-3.1-flash-live-preview}
      - GEMINI_LIVE_VOICE=${GEMINI_LIVE_VOICE:-Kore}

  frontend:
    build: ./frontend
    container_name: agrilog-frontend
    restart: always
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://backend:3000
    depends_on:
      - backend

